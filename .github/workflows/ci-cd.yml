name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ununtu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up .NET
        uses: action/setup-dotnet@v3
        with:
          dotnet-version: "9.0.x"

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build --no-restore --configuration Release

      - name: Run tests
        run: dotnet test --no-build --configuration Release --verbosity normal

  build-docker:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: action/checkout@v3

      - name: build docker image
        run: |
          docker build -t my-auth-service:latest ./AuthService
          docker build -t my-user-profile-service:latest ./UserProfileService
          docker build -t my-post-service:latest ./PostService
          docker build -t my-api-gateway:latest ./ApiGateway

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker images
        run: |
          docker push my-auth-service:latest
          docker push my-user-profile-service:latest
          docker push my-post-service:latest
          docker push my-api-gateway:latest
